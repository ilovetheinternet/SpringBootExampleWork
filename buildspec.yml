version: 0.2




phases:
  install:
    commands:
      - echo Maybe it needs an install command. let us try adding one before adding the artifacts section.
  build:
    commands:
      - ./gradlew bootJar
